---
description: Load Next.js best practices skill — file conventions, RSC boundaries, data patterns, metadata, error handling, route handlers.
---

# /nextjs — Next.js Best Practices

Apply Next.js best practices to the current task or code.

## Instructions

1. Load `.agents/skills/next-best-practices/SKILL.md` for the full reference.
2. Apply the skill's guidance — file conventions, RSC boundaries, data patterns, async APIs, metadata, error handling, route handlers, image/font optimization, bundling.
3. For sub-topics, also load the relevant reference file from the skill directory (e.g. `rsc-boundaries.md`, `data-patterns.md`).

## Arguments

`$ARGUMENTS` — the Next.js question, task, or code to review. If empty, ask what the user needs help with.
