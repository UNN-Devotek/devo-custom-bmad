---
description: Load WebSocket engineering skill — Socket.IO, horizontal scaling with Redis, presence tracking, room management.
---

# /websocket — WebSocket Engineering

Apply WebSocket engineering patterns to the current task.

## Instructions

1. Load `.agents/skills/websocket-engineer/SKILL.md` for the full reference.
2. Apply the skill's guidance — bidirectional messaging, Socket.IO patterns, horizontal scaling with Redis adapter, presence tracking, room management, reconnection strategies.

## Arguments

`$ARGUMENTS` — the WebSocket question, task, or architecture to review. If empty, ask what the user needs help with.
