---
description: Load Next.js App Router patterns skill — Server Components, streaming, parallel routes, advanced data fetching for Next.js 14+.
---

# /nextjs-app-router — Next.js App Router Patterns

Apply Next.js 14+ App Router patterns to the current task.

## Instructions

1. Load `.agents/skills/nextjs-app-router-patterns/SKILL.md` for the full reference.
2. Apply the skill's guidance — Server Components, streaming, parallel routes, intercepting routes, advanced data fetching patterns.

## Arguments

`$ARGUMENTS` — the App Router question, task, or code to review. If empty, ask what the user needs help with.
