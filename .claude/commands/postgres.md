---
description: Load PostgreSQL optimization skill — JSONB, arrays, custom types, full-text search, window functions, extensions.
---

# /postgres — PostgreSQL Optimization

Apply PostgreSQL-specific optimization to the current task or query.

## Instructions

1. Load `.agents/skills/postgresql-optimization/SKILL.md` for the full reference.
2. Apply the skill's guidance — JSONB operations, array types, range/geometric types, full-text search, window functions, PostgreSQL extensions, query optimization.

## Arguments

`$ARGUMENTS` — the PostgreSQL question, query, or schema to review. If empty, ask what the user needs help with.
