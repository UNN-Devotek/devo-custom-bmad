---
description: Load TypeScript best practices skill — strict mode, discriminated unions, type-safe patterns for TypeScript 5.x.
---

# /typescript — TypeScript Best Practices

Apply TypeScript best practices to the current task or code.

## Instructions

1. Load `.agents/skills/typescript-best-practices/SKILL.md` for the full rule set.
2. Apply the skill's patterns to the user's request — type safety, strict mode, discriminated unions, const assertions, satisfies operator.
3. When reviewing code, flag violations of the patterns in the skill.

## Arguments

`$ARGUMENTS` — the TypeScript question, task, or code to review. If empty, ask what the user needs help with.
