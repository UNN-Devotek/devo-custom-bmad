---
description: Load Python backend skill — production-ready patterns for FastAPI, SQLAlchemy, and Upstash.
---

# /python-api — Python Backend Patterns

Apply production Python backend patterns to the current task.

## Instructions

1. Load `.agents/skills/python-backend/SKILL.md` for the full reference.
2. Apply the skill's guidance — FastAPI patterns, SQLAlchemy models, Upstash integration, production-ready backend architecture.

## Arguments

`$ARGUMENTS` — the backend question, task, or code to review. If empty, ask what the user needs help with.
